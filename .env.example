# transport-performance-analysis currently reads no environment variables —
# a pandas/Jupyter analysis over the local synthetic dataset in data/. Kept
# as a template per the project root interface (nqwrc.dots setup/01,
# "Standard Internal Structure for Every Project Folder") for whenever the
# pipeline needs credentials or an external data source.
